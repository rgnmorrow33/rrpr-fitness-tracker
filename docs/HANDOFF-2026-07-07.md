# Handoff - System Hardening - 2026-07-07

Branch: `system-hardening` (local only, branched from `main` at `a510136`).
Nothing has been pushed. Main is untouched. All work below came out of the
2026-07-07 setup audit ("which file enforces this tomorrow?").

## What changed and why

### 1. v4.35 tagged (audit finding: tag rule violated in production)

v4.35 shipped to production at `a510136` but was never tagged - the exact
failure the "a version is not done until it is tagged" rule was written to
prevent. Tag `v4.35` now exists locally and points at
`a5101367bb95bd0a61cdd0d989a07b669556d3b6`.

**Pending at review:** the tag is local only (no-push ground rule). Push it
with `git push origin v4.35`. A tag push does not deploy anything - the
commit is already live.

### 2. Pre-push syntax gate - `scripts/hooks/pre-push-syntax-check.js`

Why: "always run node --check before push" had no backstop, and CI
(smoke.yml) runs post-deploy, so a syntax error would reach Selisa's iPads
~30 seconds after a bad push.

What it does: a Claude Code PreToolUse hook. When a Bash/PowerShell command
containing `git push` is about to run, it extracts every inline `<script>`
block from `RoundRock_Fitness_Tracker.html` **as of HEAD** (the content the
push would ship, not the working tree), pads line numbers so errors report
real HTML line numbers, and runs `node --check`. Failure blocks the push
(hook exit 2) and feeds the syntax error back to Claude. It also blocks if
the tracker file is missing/renamed at HEAD, which incidentally enforces the
"do NOT rename the tracker file" rule. Unexpected hook errors fail OPEN
(exit 1) so a bug in the guard can never freeze all pushes.

### 3. Pre-push tag gate - `scripts/hooks/pre-push-tag-check.js`

Why: the tag rule lived only in CLAUDE.md prose and demonstrably failed
(v4.35).

What it does: same PreToolUse shape. On `git push`, it scans commit messages
in the outgoing range (`origin/<branch>..HEAD`, falling back to
`origin/main..HEAD` for new branches) for `vX.Y` references and blocks if
any referenced version has no matching git tag, printing the exact commands
to fix it.

**Convention shift:** this enforces tag-BEFORE-push. CLAUDE.md currently
says tag AFTER the push. The tag still marks the exact commit that goes
live; the order just flips (tag locally, push branch, push tag). CLAUDE.md's
"Version tagging" section needs a wording update - deferred to the CLAUDE.md
diet session.

### 4. Hook wiring tracked in-repo - `.claude/settings.json` + `.gitignore`

Why: the entire setup was device-local; if this machine dies the enforcement
dies with it.

`.claude/settings.json` (now tracked) registers both hooks under
`PreToolUse` with matcher `Bash|PowerShell`. `.gitignore` changed from
ignoring `.claude/` wholesale to `.claude/*` + `!.claude/settings.json`, so
the shared config is versioned while device-local files
(`settings.local.json`, proposals, caches) stay ignored - verified via
`git check-ignore`.

### 5. Permission flattening - APPLIED (approved by Reagan 2026-07-07)

Flattened lists were staged as `.proposed` files, reviewed, approved, then
copied over the live files (verified byte-identical via diff; both parse as
valid JSON). The `.proposed` files were deleted after apply. Note: both live
files are gitignored, so this change exists only on this machine - reverting
means re-adding entries by hand or from this document.

- `.claude/settings.local.json` - project level, 107 entries -> 11.
  Removed: ~90 dead one-shot literal commands (exact historical commit
  messages, full-path git invocations superseded by `PowerShell(git *)`),
  plus `PowerShell(Remove-Item *)`, `PowerShell(Set-Content *)`,
  `PowerShell(winget install *)` as instructed. Judgment call included for
  veto: `PowerShell(Out-File *)` and `PowerShell(Write-Error *)` also
  dropped (Out-File is the same overwrite class as Set-Content). Added
  narrow durable entries: `PowerShell(node --check *)`,
  `PowerShell(npm run *)`.
- `C:\Users\rmorrow\.claude\settings.local.json` - user level,
  22 entries -> 6. Removed: the three curl entries with baked-in Supabase
  JWTs (as instructed), one-shot awk/python/grep commands, and node --check
  temp-file entries superseded by `Bash(node *)`. `git commit -m ' *`
  widened to `Bash(git commit *)` (covers `-F`, `--amend`).

Final project list: `PowerShell(git *)`, `Test-Path *`, `Get-ChildItem *`,
`Get-Content *`, `Get-Command *`, `Get-Item *`, `Select-String *`,
`Join-Path *`, `New-Item *`, `node --check *`, `npm run *`.
Final user list: `Read(//c/Docs/rrpr-fitness-tracker//**)`,
`Bash(git add *)`, `Bash(git commit *)`, `Bash(git push *)`,
`Bash(git status *)`, `Bash(node *)`.

## Test evidence (all run 2026-07-07 on this branch)

Tag:

    $ git rev-parse v4.35^{commit}
    a5101367bb95bd0a61cdd0d989a07b669556d3b6
    $ git log -1 --oneline v4.35
    a510136 fix(fms): v4.35 polish - modal widths, composite prominence, history expand

Syntax hook (invoked with the exact stdin JSON Claude Code sends):

    clean HEAD + "git push"        -> exit 0 (allowed)
    non-push command ("git status")-> exit 0 (ignored)
    HEAD with `const __INTENTIONALLY_BROKEN = ;` committed at HTML line 2056
                                   -> exit 2 (BLOCKED), stderr:
       "BLOCKED: node --check failed on the embedded JS in
        RoundRock_Fitness_Tracker.html at HEAD. ...
        RoundRock_Fitness_Tracker.html (embedded JS):2056
        const __INTENTIONALLY_BROKEN = ;  ... SyntaxError: Unexpected token ';'"
    (an off-by-one in the reported line number was found during this test and
    fixed - the retest above shows the corrected 2056)
    temp commit reset via `git reset --hard HEAD~1`; clean retest -> exit 0

Tag hook:

    empty push range               -> exit 0 (allowed)
    temp commit "TEMP: fix(test): v9.99 polish" -> exit 2 (BLOCKED):
       "BLOCKED: commit(s) in origin/main..HEAD reference version(s) with no
        git tag: v9.99. ... git tag v9.99 / git push / git push origin v9.99"
    temp commit mentioning v4.35 (tagged) -> exit 0 (allowed)
    both temp commits reset; HEAD back at a510136

Tracking:

    $ git check-ignore .claude/settings.local.json .claude/settings.json
    .claude/settings.local.json        <- only the local file is ignored
    settings.json validated as parseable JSON

## Known limitations / activation notes

- Hooks registered in settings load at session start: they become active
  the NEXT Claude Code session (review them in the /hooks menu). The scripts
  themselves are fully tested above via the real stdin contract.
- These are Claude Code hooks: they gate pushes made THROUGH Claude Code.
  A manual `git push` from a plain terminal bypasses them. If that matters,
  a follow-up can add a tracked githooks/ dir + `git config core.hooksPath`
  for git-level enforcement on any machine.
- Hook commands use paths relative to the project root (the normal hook
  cwd). Both scripts also self-resolve the repo root via CLAUDE_PROJECT_DIR
  / `git rev-parse` for the files they inspect.
- The tag hook only reads commits on the current branch's outgoing range;
  it does not parse push refspecs (pushing a ref other than HEAD is not this
  repo's flow).

## What remains (deferred to follow-up sessions)

1. CLAUDE.md diet: move the ~7KB Deferred cleanup pile / Deferred features
   backlog out of the always-loaded file; purge its own line-number refs;
   retire stale pile entries (the SCHEMA.md line-ref sweep already happened).

2. CLAUDE.md tag-rule wording: the "Version tagging (tag on release)"
   section says tag AFTER the version's commits are pushed to main. The
   pre-push-tag-check hook enforces the opposite order: tag BEFORE pushing
   (tag locally, push branch, push tag). Rewrite that section to
   tag-before-push so the documented convention matches what the hook
   actually enforces. The rationale paragraph and the "no annotated tags"
   rule are still correct; only the ordering sentences change.

3. ARCHITECTURE.md section 6 realtime fix: it claims 12 tables publish live;
   the supabase_realtime publication contains only notifications and
   trainer_time_off. Align it with CLAUDE.md's verified reality.

4. Push tag v4.35 to origin; merge this branch after review.

5. Git-level hook coverage, so manual terminal pushes get the same syntax
   and tag gates (the Claude Code hooks only fire on pushes made through
   Claude Code). Implementation notes - executable cold:

   a. Add a `--git` mode to both existing scripts. In
      `scripts/hooks/pre-push-syntax-check.js` and
      `scripts/hooks/pre-push-tag-check.js`, at the top of `main()`, before
      the stdin read: `if (process.argv.includes('--git')) { runCheck(); }`
      where `runCheck()` is the existing logic minus the stdin-JSON parse
      and minus the `isGitPush()` gate (in --git mode git itself guarantees
      a push is happening). Everything from `projectRoot()` onward is
      reusable as-is. Exit codes in --git mode: 0 = allow, ANY nonzero =
      git blocks the push, so a detected problem should exit 1 (not 2 -
      exit 2 has no special meaning to git). Keep the fail-open design:
      wrap `runCheck()` so an unexpected internal error prints a warning
      and exits 0 (in Claude-hook mode fail-open is exit 1; in git mode
      fail-open must be exit 0 because git treats nonzero as block).
   b. Create `githooks/pre-push` (no file extension, POSIX sh - Git for
      Windows runs hooks under its bundled sh, and `node` is on PATH):

          #!/bin/sh
          # Repo-tracked pre-push gates. Activated per-clone via:
          #   git config core.hooksPath githooks
          node scripts/hooks/pre-push-syntax-check.js --git || exit 1
          node scripts/hooks/pre-push-tag-check.js --git || exit 1
          exit 0

      Commit it with the executable bit: `git add githooks/pre-push` then
      `git update-index --chmod=+x githooks/pre-push` (Windows does not set
      it from the filesystem).
   c. Activation is one command per clone/machine (git config is not
      versioned): `git config core.hooksPath githooks`. Document that
      command in CLAUDE.md's Deployment pipeline section and run it on this
      machine. Note: setting core.hooksPath means git IGNORES .git/hooks
      entirely - fine here, .git/hooks contains only samples.
   d. Test the same way this session tested the Claude hooks: commit an
      intentional syntax error, run `git push --dry-run` against a local
      bare remote (`git init --bare /tmp/rrpr-test.git; git remote add
      testremote /tmp/rrpr-test.git; git push testremote HEAD`), confirm
      the push is rejected, reset the temp commit, remove the test remote.
      (A dry-run still fires pre-push, so no real remote is ever touched.)
   e. Cwd caveat: the hook script cds nowhere; git runs pre-push from the
      repo root, so the relative `scripts/hooks/...` paths are safe.

6. Optional: CI grep for the writing conventions (em dash found in
   user-facing label ' PT — '; "staff" currently clean).
